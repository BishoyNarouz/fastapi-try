from prisma.models import User
from typing import Optional, List
from prisma import Prisma, types


# class UserRepository:

    # async def find_one(where: types.UserWhereUniqueInput, include: Optional[types.UserInclude] = None) -> Optional[
    #     User]:
    #
    #     return await User.prisma().find_unique(where={}, include=)
